package org.example.Utils;

public class SchedulePolicyService {
    public void createPolicy(SchedulePolicy policy) {
        // Define work shift parameters
        System.out.println("Shift policy created: " + policy.getPolicyName());
    }
}
